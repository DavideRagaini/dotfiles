# -*- mode: sh; -*-

# Additional command line arguments for picom(1).
OPTS=''
# OPTS='--dbus'

# Setting this to 1 enables logging, any other value - disables.
LOGGING_ENABLE=0
# The directory will be created for you, if logging is enabled.
LOG_DIR="${XDG_DATA_HOME:-$HOME/.local/share}/log/picom"
